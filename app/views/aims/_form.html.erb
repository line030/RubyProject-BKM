<%= form_for(@aim) do |f| %>
  <% if @aim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aim.errors.count, "error") %> prohibited this aim from being saved:</h2>

      <ul>
      <% @aim.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

    <div class="field">
      <%= f.label :value %><br />
      <%= f.text_field :value %>
    </div>

   <div class="field">
     <%= f.label :category_id %><br />
     <%= select("aim", "category_id", @category_selection_list,
                 {:prompt =>  "Select Category"  } ) %>
   </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
