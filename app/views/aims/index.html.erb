<h1>Listing aims</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Value</th>
    <th>Einheit</th>
    <th>Category</th>
    <th></th>
    <th></th>
  </tr>

<% @aims.each do |aim| %>
  <tr>
    <td><%= aim.name %></td>
    <td><%= aim.value %></td>
    <td>
    <% if !aim.category.nil? %>
        <%= aim.category.unit.name %>
    <% else %>
      <%= "No Unit" %>
    <% end %></td>
    <td>
      <% if aim.id.nil? %>
        <%= @categories[aim.id] %>
      <% else %>
          <%= "No Category" %>
        <% end %>
    </td>

    <td><%= link_to 'Show', aim %></td>
    <td><%= link_to 'Edit', edit_aim_path(aim) %></td>
    <td><%= link_to 'Destroy', aim, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Aim', new_aim_path %>
